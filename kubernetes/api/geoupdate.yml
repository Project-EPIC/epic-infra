apiVersion: apps/v1
kind: Deployment
metadata:
  name: geo-update-api
  labels:
    app: geo-update-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: geo-update-api
  template:
    metadata:
      labels:
        app: geo-update-api
    spec:
      terminationGracePeriodSeconds: 10
      volumes:
         - name: keyfiles
           secret:
             secretName: keyfile
      containers:
      - name: geo-update-api
        image: projectepic/geoupdateapi:1.0.1
        imagePullPolicy: Always
        volumeMounts:
         - mountPath: /private
           name: keyfiles
        resources:
          limits:
           memory: 1000Mi
           cpu: "150m"
          requests:
           memory: 500Mi
           cpu: "20m"
        ports:
          - containerPort: 8080
          - containerPort: 8081
        livenessProbe:
          periodSeconds: 60
          httpGet:
            path: /healthcheck
            port: 8081
          initialDelaySeconds: 120
          timeoutSeconds: 5
        readinessProbe:
          periodSeconds: 20
          httpGet:
            path: /healthcheck
            port: 8081
          initialDelaySeconds: 40
          timeoutSeconds: 5
        env:
         - name: GOOGLE_APPLICATION_CREDENTIALS
           value: /private/keyfile.json
---
apiVersion: v1
kind: Service
metadata:
  name: geo-update-api
spec:
  type: NodePort
  ports:
  - port: 8080
    name: client
  selector:
    app: geo-update-api
